spring.application.name=Internproject
# Server port (Railway/Render sets PORT env variable)
server.port=${PORT:8080}
# Database configuration (use environment variables for production)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/Revision}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Sowdesh@21}


spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
